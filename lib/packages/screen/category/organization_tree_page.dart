import 'package:flutter/material.dart';

class OrganizationTreePage extends StatelessWidget {
  const OrganizationTreePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('TREE HERE')));
  }
}
